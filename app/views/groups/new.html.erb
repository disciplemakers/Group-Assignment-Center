<% content_for :title do "GAC - Create New Group" end %>

<h3>New group<% if @parent %> in <span class="normal-case"><%= @parent.name %></span><% end %></h3>

<%= render 'form' %>

<%= link_to 'Back', groups_path %>

<%= form_for(@location_relation) do |f| %>
  <% if @location_relation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@location_relation.errors.count, "error") %> prohibited this location_relation from being saved:</h2>

      <ul>
      <% @location_relation.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :parent_id %><br />
    <%= f.text_field :parent_id %>
  </div>
  <div class="field">
    <%= f.label :child_id %><br />
    <%= f.text_field :child_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

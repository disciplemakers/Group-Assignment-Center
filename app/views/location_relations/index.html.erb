<h1>Listing location_relations</h1>

<table>
  <tr>
    <th>Parent</th>
    <th>Child</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @location_relations.each do |location_relation| %>
  <tr>
    <td><%= location_relation.parent_id %></td>
    <td><%= location_relation.child_id %></td>
    <td><%= link_to 'Show', location_relation %></td>
    <td><%= link_to 'Edit', edit_location_relation_path(location_relation) %></td>
    <td><%= link_to 'Destroy', location_relation, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Location relation', new_location_relation_path %>

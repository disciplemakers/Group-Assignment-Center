<%= select_tag("left_side", groups_with_people_options_with_style(@drilldown_group).html_safe,
               {:multiple => true, :size => 16}) %>
<%= hidden_field_tag 'drilldown_group_id', @drilldown_group.id %>

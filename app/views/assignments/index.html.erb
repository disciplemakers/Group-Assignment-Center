<h1>Listing assignments</h1>

<table>
  <tr>
    <th>Group</th>
    <th>Person</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @assignments.each do |assignment| %>
  <tr>
    <td><%= assignment.group_id %></td>
    <td><%= assignment.person_id %></td>
    <td><%= link_to 'Show', event_assignment %></td>
    <td><%= link_to 'Edit', edit_event_assignment_path(assignment) %></td>
    <td><%= link_to 'Destroy', event_assignment, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Assignment', new_event_assignment_path %>
